const showElement = {
    display: "block",
}
const hideElement = {
    display: "none",
}
const showLine = {
    textDecoration: "line-through",
}
const hideLine = {
    textDecoration: "none",
}
export {showElement, hideElement, showLine, hideLine};